# Compiled content store -- derived from scripts/content/*.py, never pushed
data/processes.json
data/.diagram-hashes.json

# Mermaid sources are build artifacts; the PNGs in assets/img are what the site serves
diagrams/

# Local tooling
.claude/
.DS_Store
__pycache__/
*.pyc
node_modules/
logs/
data/.generation_failures.json
